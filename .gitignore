
# Playwright
node_modules/
/test-results/
/playwright-report/
/playwright-report-jf575/
/blob-report/
/playwright/.cache/
/playwright/.auth/
/.auth/
TestByAghbar/.auth/

# Environment / secrets — never commit real credentials
.env
.env.*
!.env.example

# Debug artifacts — screenshots/diagnostics captured during exploration, never committed
*.png
Automation-Tests/debug-*
Automation-Tests/diag-*
Automation-Tests/diagnose-*

# QA UI verification screenshots — local only, never committed
qa-artifacts/**/evidence/

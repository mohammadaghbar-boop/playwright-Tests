{
  "name": "jf-real-life-scenarios",
  "version": "1.0.0",
  "private": true,
  "description": "Persona-driven end-to-end user-journey tests for the Infath Joint Funds portal — real users, real flows. Separate from the regression pack.",
  "scripts": {
    "test": "playwright test",
    "test:list": "playwright test --list",
    "report": "playwright show-report",
    "typecheck": "tsc --noEmit",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^24.0.0",
    "dotenv": "^17.2.3",
    "typescript": "^5.9.3"
  }
}
